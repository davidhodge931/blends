% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/blends.R
\name{blend}
\alias{blend}
\title{Blend colours and palettes, with an automatic default mode}
\usage{
blend(..., blend = NULL)
}
\arguments{
\item{...}{Either one or two colour/palette arguments:
\itemize{
\item If one argument: the colour or palette is blended with itself
\item If two arguments: the first is blended with the second
Each argument can be a character vector of colours or a \verb{scales::pal_*()}
function.
}

Recycling for direct colour vectors follows \code{vctrs} semantics: inputs must
either have the same size, or one input must have size 1.}

\item{blend}{A blend function such as \code{multiply}, \code{screen}, \code{overlay}, or
any custom blend function sharing the same \code{...} colour/palette
interface. If \code{NULL} (the default), the mode is chosen automatically
based on panel background darkness.}
}
\value{
Character vector of blended colours or a blending function.
}
\description{
By default, chooses between \code{screen()} and \code{multiply()} based on the
current ggplot2 theme's panel background: \code{screen()} is used when the
panel background is dark, and \code{multiply()} is used otherwise (including
when ggplot2 isn't installed, no panel background is set, or the theme
can't be determined).

Any other blend mode can be used instead by supplying \code{blend} — this
accepts \code{multiply}, \code{screen}, \code{overlay}, \code{hard_light}, \code{soft_light},
\code{colour_burn}, \code{colour_dodge}, \code{darken}, \code{lighten}, \code{difference},
\code{exclusion}, or any custom blend function built with
\code{blends:::.new_blend()}. \code{blend()} is the recommended everyday entry
point into the package: reach for it (or \code{multiply()} directly) unless
you specifically need a different blend mode.

The \code{blend} argument here matches the \code{blend} argument used throughout
the \verb{scale_blend_*()} family (e.g. \code{scale_blend_discrete(blend = ...)}),
so the same value can be passed to either.
}
\examples{
blend("#F0F0F0", "#808080")
blend("#FF6B6B")
blend("#FFA600FF", "#8991A1FF", blend = overlay)
}

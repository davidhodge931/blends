% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scale.R
\name{scale_blend_continuous}
\alias{scale_blend_continuous}
\title{Blend continuous fill and colour scales}
\usage{
scale_blend_continuous(palette = NULL, blend = NULL, aesthetic = "colour", ...)
}
\arguments{
\item{palette}{A continuous colour palette function used for the \emph{base}
aesthetic (i.e. whichever of \code{colour}/\code{fill} is not named in
\code{aesthetic}). Defaults to the current theme's
\code{palette.colour.continuous} or \code{palette.fill.continuous} (matching the
base aesthetic), falling back to \code{scales::pal_seq_gradient()}.}

\item{blend}{A function that takes a palette and returns a blended
palette (e.g. \code{multiply}, \code{screen}, \code{overlay}). If \code{NULL}
(the default), the blend mode is chosen automatically based on
whether the panel background is dark or light (see \code{blend()}).

The blended palette is always derived from the base palette blended
with itself, so blend modes whose output is identity or constant when
both inputs are equal (namely \code{darken}, \code{lighten}, and \code{difference})
are not useful here and should be avoided.}

\item{aesthetic}{Which aesthetic ("colour" or "fill") receives the
blended palette. The other aesthetic receives the base \code{palette}.
Defaults to \code{"colour"}, i.e. fill is the base and colour is blended
from it. \code{"color"} is accepted as an alias for \code{"colour"}.}

\item{...}{Additional arguments passed on to \code{ggplot2::scale_fill_continuous()}
and \code{ggplot2::scale_colour_continuous()}.}
}
\value{
A list of two ggplot2 scales: a fill scale and a colour scale.
}
\description{
Creates matching continuous \code{fill} and \code{colour} scales, where one
aesthetic is treated as the "base" palette and the other is derived from
it by applying a blend mode. If no \code{blend} function is supplied, the
blend mode is chosen automatically: \code{screen()} is used when the
set theme panel background is dark, and \code{multiply()} is used
otherwise.
}
\examples{
\dontrun{
ggplot(faithfuld, aes(waiting, eruptions, colour = density, fill = density)) +
  geom_point(shape = 21, size = 3) +
  scale_blend_continuous()

ggplot(faithfuld, aes(waiting, eruptions, colour = density, fill = density)) +
  geom_point(shape = 21, size = 3) +
  scale_blend_continuous(blend = overlay)
}
}
